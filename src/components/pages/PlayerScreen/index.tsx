import React from 'react';
import PlayerScreenTemplate from '@components/templates/PlayerScreenTemplate';

const PlayerScreen: React.FC = () => {
  return <PlayerScreenTemplate />;
};

export default PlayerScreen;

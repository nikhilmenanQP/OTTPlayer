export interface PlayerState {
  controlsVisible: boolean;
  currentTime: number;
  duration: number;
  isErrorVisible: boolean;
  isFullscreen: boolean;
  isLoading: boolean;
  isSliding: boolean;
  paused: boolean;
}

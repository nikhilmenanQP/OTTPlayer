/**
 * @type {Style}
 * StyleSheet object for css of AppGradient
 */
import {StyleSheet} from 'react-native';
import {Theme} from '@context/ThemeProviderContext/types';

export const createStyle = (theme: Theme) =>
  StyleSheet.create({
    // Add your style here...
  });

import {TextStyle, ViewStyle} from 'react-native';

export interface DescriptionTextProps {
  containerStyle: ViewStyle;
  numberOfLines: number;
  showMoreTextStyle: any;
  text: string;
  textStyle: TextStyle;
}
